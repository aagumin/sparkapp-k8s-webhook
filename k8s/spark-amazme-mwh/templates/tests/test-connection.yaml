apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spark-amazme-mwh.fullname" . }}-test-connection"
  labels:
    {{- include "spark-amazme-mwh.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spark-amazme-mwh.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
